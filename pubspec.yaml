name: presto_first
description: "Sistema de prestamos bibliotecarios"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  animate_do: ^5.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_native_splash: ^2.4.8
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/presto_logo2.jpeg

flutter_native_splash:
  color: "#ffffff"
  image: assets/images/presto_logo2.jpeg
  android: true
  ios: true